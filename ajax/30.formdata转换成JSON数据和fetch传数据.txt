增删改查必须是fetch提交json格式 传回来json格式mongodb点新增时执行add方法 把template显示到div中点post提交时    先把formdata中输入的数据取出来	然后转为json 用fetch提交到服务器两个知识点1.如何formdata的数据转为json data=new FormData(form1);json={}data.forEach((v,k) => {    json[k]=v;});2.fetch提交jsonfetch('/users',{    method:'post',    body:JSON.stringify(json),    headers:{       'content-type':'application/json;charset=utf-8'    } }).then(res=>res.json()).then(res=>{     alert(res.errmsg);     location.reload(); }) 